import { PageContainer } from '@ant-design/pro-components';

const Login: React.FC = () => {
    return (
        <PageContainer ghost>
            login
        </PageContainer>
    );
};

export default Login;
